单个能力分析

通常用户会提供一个或者多个代码目录，让分析其中的一个特定能力机制，再得出一篇分析报告。

语言风格

- 少用专门名词，只有没有合适的替代名词时才用专业名词；能用大白话就用大白话。大白话指的是计算机专业的普通从业者能听懂的说法，比如进程/线程/agent/幂等/鉴权，就是普通名词；坏的例子是"exactly-once 语义边界"这种只有特定领域才用的说法。
- 讲解机制时不能说函数名/变量名/文件名/代码路径，重点说"干了什么"，而不是"哪个函数调了哪个函数"。
- 正确：启动 opencode server 并读取 stdout 输出解析得到 url。
- 错误：createOpencodeServer 用 cross-spawn 启动 opencode serve，默认 127.0.0.1:4096，并设置 OPENCODE_CONFIG_CONTENT，证据 packages/sdk/js/src/server.ts:22-40。它读取 stdout 的 opencode server listening 行解析 URL，证据 :43-70；超时、exit、error 会 reject，证据 :75-91。

你可以自由探索完成这个任务，按照你认为合理的方式来输出报告。但是，输出的报告开头必须先写 `项目地址：<GitHub URL>`，并且至少要包含如下内容。项目地址必须来自用户提供的链接或仓库 Git remote 的核验结果，不能凭项目名猜测；若无法核验，明确写 `项目地址：未发现可核验的 GitHub 地址`。

1. 能力版本表

这个能力有哪些版本。一些复杂项目里，可能有多种启动方式，不同启动方式这部分的能力不一样，也可能有多个版本，有的没用。

这部分用表格明确说明哪些能力被哪块场景用了，并为每一列命名。
后续所有能力介绍都必须归类到哪一列里。

2. 名词表

面向不懂这个项目的科班计算机中级工程师来写，本文出现的所有项目级名词/概念/定义都必须在这里有过解释。

表应该包含 | 通用名词 | 本仓库的叫法 | 解释 |。
统一名词：名词表中不能重复同一个概念，也不能让同一个概念出现两个名字；如果原文有两个名字，用 name1/name2 写在同一行。
充分的解释：每个解释都必须包含完整的主语、谓语和宾语，用大白话解释，不使用专业术语。

3. 能力拆解

这个能力能拆解吗，他是一个基础的原始能力，还是包含了多个小能力。

这里的能力一定是连计算机专业的产品都能听懂的业务能力，而非技术能力。

正确：提供定时器的增删改查。
错误：调用 os 的 timer()。

4. 技术机制

为了实现这个能力，增加了哪些技术机制。

这里应该按照一个懂开发，但是不懂这个项目的科班计算机中级工程师的受众来写，不应该用本仓库的具体函数/名字/变量/文件名/代码路径，而是用计算机的通用知识。

正确：使用写本地的 json 文件来让定时任务持久化，重启时重新加载 json 来恢复定时任务。
错误：用 JSON5 解析到 ~/.openclaw/cron/jobs.json，Gateway 重启时重新加载 store、重新 arm 定时器并做补偿。

5. 机制组成

按照代码组织的方式，介绍有哪些模块/对象/服务共同完成这个能力的。

用表格，| 名字 | 类型 | 职责 | 关系 |。

关系里介绍和其他几个模块的交互关系，例如调用、持有、包含等。

6. 能力整体介绍（按交互粒度）

一个能力可以分为：

计算（做校验验证文件是否损坏）
数据处理（内存中对象记录）
os 交互（写入文件、创建进程）
网络交互（请求其他服务，典型的就是数据库）

按照这个粒度来整体介绍这个能力；不要直接把代码中函数的调用翻译成中文。

正确：阻塞调用 filesync 服务从 bos 拉取数据到工作区，完成后，用 md5 校验文件，解压文件，将文件移动到各自的文件目录，删除压缩包和 md5，结束文件下载。
错误：宿主 App 启动线程时，在 thread-start 参数里传入 dynamicTools 数组，server/src/request_processors/thread_processor.rs:202, 1168-1204 校验/计数。

7. 仓库完成度

重点核验本次子能力，明确核验范围，不能把局部结论扩展为全仓验收。

机制说明讲清楚行为，具体代码位置、测试和 PR 链接放在证据列或段落末尾。

判断文档说明的能力在当前代码中完成到了哪一步。先记录分析日期、分支或提交、影响分析的未提交改动，以及本次核验的能力和运行环境；区分当前默认产品、可选特性和未来设计目标。

### 核验与输出

- 按用户能观察到的能力，对照 README/设计说明、真实调用入口、实现和测试。重点核查入口是否接通、默认构建是否包含功能，以及平台或配置条件；接口存在、只打印计划、模拟返回都不能等同于实际执行。
- 用表格呈现：`| 能力 / 场景 | 文档声明 | 实际实现 | 验证证据 | 完成度与缺口 |`。完成度使用“主体完成 / 部分完成 / 未实现”；无法确认时写“证据不足”，不把“本次没测”当作“没有实现”。已明确标为未来目标的项目，要标注其规划性质。
- 区分源码判断、已有测试内容、本次实际测试、目标环境端到端验证和 CI 结果。不要把测试名称、测试数量或 CI 绿灯当作真实用户链路已通过；检查关键测试是否只覆盖参数、模拟服务或因环境条件跳过。
- 按任务需要运行有代表性的构建、测试或无害探针，不要求每次全量重跑。写清测试范围、结果和未验证项；环境阻碍应记录原因，不能改锁文件或弱化校验来制造“通过”。
- 总结功能实现、产品接通和部署验证三个方面的完成程度，列出影响使用的主要缺口。没有清晰验收清单和计算口径时，不给伪精确的整体百分比；若评分，说明分母、权重和不确定性。

更新已有报告时，先比较旧基线与新代码，将结论分成“新增 / 已解决 / 仍存在 / 未重新验证”，避免把旧测试结果写成本次新结果。

8. 近期迭代方向（最近 10 个 PR 与近一个月取较少）

按下述数量与时间范围取仓库级样本，先归纳仓库整体方向，再说明与本次子能力的关系；不要仅取相关 PR 后仍称其为仓库级近期样本。没有相关变化时如实说明。

目的不是复述提交标题，而是说明维护者最近在推进什么，以及这些变化如何影响当前能力和完成度。

### 取样口径

- 默认比较“最近 10 个已合并 PR”和“近一个月内合入默认分支的 PR”，**取 PR 数量较少的范围**；等价于按合并时间倒序，最多取近一个月内的 10 个 PR，并分析其实际合入的代码。近一个月以查询时点向前回溯一个月，报告写明起止日期、时区、目标分支和实际样本数量。用户指定其他范围时按其要求执行。不要用最近更新时间或 PR 编号代替合并时间。
- 例如：近一个月有 6 个 PR，就分析这 6 个；有 25 个，就分析最近 10 个；没有已合并 PR，就说明该时间窗口没有 PR 样本，不向更早历史补足。
- 从已核验的仓库托管平台获取真实 PR 数据，使用可用的只读 API、CLI 或连接器，必要时分页。GitLab MR、iCode CR 等可作为对应平台的评审单，保留原称和可核验链接，不伪称 GitHub PR。
- 默认不混入未合并、已关闭未合并的 PR；若它们能解释下一步方向，可在样本之外单独说明“进行中/尚未落地”。自动依赖更新、文档 PR 也保留在样本内，并按实际影响分类。
- 权限不足、平台无 PR 或仅能取得本地历史时，明确说明限制；可分析近一个月内的提交代码作为补充线索，但必须标为“提交历史替代分析”，说明提交数量、时间口径及无法核验 PR 数量。不得把 commit 数量当作 PR 数量，也不得据此认定仓库没有 PR。

### 分析与输出

1. 阅读每个样本的标题、正文和改动文件；对于决定能力变化或完成度判断的 PR，进一步查看关键 diff 和测试。只有标题或元数据时，明确降低结论置信度。
2. 提供样本表：`| PR / MR / CR 链接 | 合并日期 | 主要改动 | 方向分类 | 对能力的影响 |`，让读者能核对所用样本。
3. 按证据归纳迭代方向，例如新能力、补齐已有链路、稳定性/安全修复、性能、接口或架构调整、工程与文档维护。提供方向表：`| 迭代方向 | 对应 PR 与数量 | 解决的问题 | 对完成度的影响 |`。同一 PR 若归多个方向，说明数量允许重叠，不把重叠计数当作总 PR 数。
4. 结合样本覆盖时间说明主要投入方向及典型变化，不从少数 PR 推断长期路线图。区分实际合入的行为、仅增加测试或文档、以及仍属计划的内容；合并不代表已发布，也不代表已包含在当前分析的本地提交中，应核查版本关系或明确尚未核验。

9. 其他你认为重要的东西

不同仓库能力对比

用户可能会发多个项目，对每一个项目都应该按上面的规则生成一个文档，之后再得到一个 main 文档，其中至少包含如下内容

这个能力服务的业务场景
共用常见名词表
这个能力常见的基础子能力
技术机制汇总
整体方案评价
其他你任务重要的东西
